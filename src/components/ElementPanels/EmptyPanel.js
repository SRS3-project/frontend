import React, { useState } from "react";


const EmptyPanel = ({ element, setInfo}) => {
	
    /* switch(param) {
        case 'barracs':
          return ;
        case 'tower':
          return TechPanel();
        case 'troops':
          null;  
        default:
          null;
      } */
 
    return (
        element
    );
};

export default EmptyPanel;
